// Authorization Data(hardcode)

login: log
password: pass

// Database settings

dbname: blog
username: delilahl
password: admin

// Database filling

drop table if exists blog;
drop table if exists admins;

create table blog (id serial primary key, title varchar(100), content varchar(400));
create table admins (login varchar(20), password varchar(20));

insert into blog (title, content) values
	('Квадратный корень', 'Арифметическим квадратным корнем из неотрицательного числа a называется такое неотрицательное число, квадрат которого равен a. Определение квадратного корня также можно представить в виде формул: Из определения следует, что a не может быть отрицательным числом.'),
	('Возведение в степень', 'Возвести число в степень - это вычисление значения степени некоторого числа. То есть слова "вычисление значение степени" и "возведение в степень" означают одно и то же. Так, если в задаче стоит "Возведите число 0,5 в пятую степень", это следует понимать как "вычислите значение степени (0,5)5 ( 0 , 5 ) 5 .'),
	('Сложение', 'Сложение (прибавление) — одна из основных бинарных математических операций (арифметических действий) двух аргументов (слагаемых), результатом которой является новое число (сумма), получаемое увеличением значения первого аргумента на значение второго аргумента. '),
	('Вычитание', 'Вычитание (убавление) — одна из вспомогательных бинарных математических операций (арифметических действий) двух аргументов (уменьшаемого и вычитаемого), результатом которой является новое число (разность), получаемое уменьшением значения первого аргумента на значение второго аргумента.');

insert into admins (login, password) values ('admin', 'admin'), ('delilahl', '123456');